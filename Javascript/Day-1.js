// what is JavaScript
// Programming language, scripting language
// runs only on browser
// single threaded synchronous language

// variables and data types
// variable -> used to store some data

// data types - primitive and referrence data type
// primitive -> string, boolean, number, undefined, null

// dynamically typed

var fname = "vasanth";
var age = 20;
var num = 10.08;
var isEligible = true;
var memoryNotCreated = undefined;
var dataNotAllocated = null;

console.log(
  typeof fname,
  typeof age,
  typeof num,
  typeof isEligible,
  typeof memoryNotCreated,
  typeof dataNotAllocated
);

// var -> function scoped, redeclare, reassign
// let -> block scoped, reassign
// const -> block scoped
